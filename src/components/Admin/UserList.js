import React from 'react';
import PropTypes from 'prop-types';

const UserList = props => {
  return (
    <div>
      <h2>User List</h2>
    </div>
  );
};

UserList.propTypes = {
  
};

export default UserList;
