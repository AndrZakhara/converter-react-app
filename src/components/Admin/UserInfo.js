import React from 'react';
// import PropTypes from 'prop-types';

const UserInfo = props => {
  return (
    <div>
      <h2>User info</h2>
    </div>
  );
};

// UserInfo.propTypes = {
  
// };

export default UserInfo;
